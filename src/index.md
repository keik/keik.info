---
title: index
layout: index.ect
---

# Index

This is index file.

A code block will be shown such like:

```js
Metalsmith('./')
  .build(function(err) {
    if (err) throw err;
    console.log('Build succeeded.');
  })
```
